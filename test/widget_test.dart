// 기존 위젯 테스트 — P0 구현 후 홈 화면 스모크 테스트로 교체
// TODO: widget test는 flutter_test의 pumpWidget 특성상
//       go_router + ProviderScope 세팅이 필요해 별도 확장 예정
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — widget tests to be added', () {
    expect(true, isTrue);
  });
}
